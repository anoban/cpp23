// non defining declarations
extern const double   pi; // the declaration must be accompanied by the extern keyword and must not have an inotializer
extern unsigned short defined_somewhere_else;

// definitions
extern double whatever {}; // definition NOT a declaration becuse we have an initializer
float         epsilon;
